## Mobile (Expo / React Native)
node_modules/
.expo/
dist/
web-build/
expo-env.d.ts
.metro-health-check*
*.tsbuildinfo

## Generated native folders (regenerated by `expo prebuild`)
/ios
/android

## Native signing / keys
.kotlin/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

## OS
.DS_Store
*.pem

## Local / temp
.home/
.tmp/

## SQL (local scripts) — note: schema files at repo root are force-added
*.sql

## Secrets (never commit)
.env
.env.*
!.env.example
!.env.*.example

## Logs
npm-debug.*
yarn-debug.*
yarn-error.*

## Admin dashboard (Node / Vite)
admin/node_modules/
admin/dist/
admin/.env
admin/.env.local
admin/.env.*.local
!admin/.env.example
