# Same keys as the Xcode project (SOCIETY/Info.plist: SUPABASE_URL, SUPABASE_ANON_KEY)
# Use the same values so both apps share one Supabase project.
SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_ANON_KEY=your-anon-key
